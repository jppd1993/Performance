/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ลบส่วน experimental ที่ไม่รองรับออก
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
